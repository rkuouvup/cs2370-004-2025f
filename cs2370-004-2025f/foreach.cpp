#include <iostream>
#include <cctype>
using namespace std;

/*int main() {
    string s;
    int count = 0;
    // ask user to give a string input
    cout << "Enter a string: ";
    cin >> s;
    // check how many upper case letters are in the string
    //for (int i = 0; i < s.length(); i++) {
    //    if (isupper(s.at(i)))
    //        count++;
    //}
    
    for (auto c : s) {
        if (isupper(c))
            count++;
    }
    
    // print the result
    cout << "There are " << count << " upper case letters" << endl;
    return 0;
}*/
