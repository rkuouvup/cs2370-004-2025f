#include <iostream>
#include <vector>
#include "point.h"
using namespace std;

/*int main() {
    //int i = 0;
    //int a[5];
    const int NPOINTS = 5;
    point_t p1 = {3.0, 4.0};
    point_t p2 = {-100, -100};
    point_t pts[NPOINTS] = {{3.5, 7.8}, {3.0, 4.0}, {2.0, 8.5}, {6.3, 8.9}, {5.0, 5.0}};
    double dist[NPOINTS] = {0.0};
    vector<double> vdist(NPOINTS);
    
    vector<point_t> vpoints = {{3.5, 7.8}, {3.0, 4.0}, {2.0, 8.5}, {6.3, 8.9}, {5.0, 5.0}};
    
    vdist_to_orig(vpoints, vdist);
    for (int i = 0; i < NPOINTS; i++)
        cout << vdist[i] << " ";
    cout << endl;
    
    
    dist_to_orig(pts, NPOINTS, dist);
    
    for (int i = 0; i < NPOINTS; i++)
        cout << dist[i] << " ";
    cout << endl;
    
    init_point(p2, 0.0, 0.0);
    cout << "p1.x: " << p1.x << endl;
    cout << "p1.y: " << p1.y << endl;
    cout << "distance between p1 and p2 is " << distance(p1, p2) << endl;
    
    return 0;
}*/
